import React from 'react';
import './Invemtory.css';

const Inventory = (props) => {
    const {_id} = props;
    return (
        <div>
            
        </div>
    );
};

export default Inventory;